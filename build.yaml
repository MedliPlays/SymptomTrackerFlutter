targets:
  $default:
    builders:
      json_serializable:
        options:
          # Nested objects (export DTOs) serialise to plain maps rather than
          # relying on jsonEncode to recurse.
          explicit_to_json: true
